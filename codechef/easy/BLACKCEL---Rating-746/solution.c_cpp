#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    // Total cells is n * n, and half of them are black
    int black_cells = (n * n) / 2;
    
    printf("%d\n", black_cells);

    return 0;
}