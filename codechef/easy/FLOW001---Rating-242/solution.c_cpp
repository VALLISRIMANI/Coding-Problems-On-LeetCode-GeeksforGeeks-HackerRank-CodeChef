#include <stdio.h>

int main() {
    int T, A, B;
    
    // Read number of test cases
    scanf("%d", &T);
    
    // Loop through each test case
    for (int i = 0; i < T; i++) {
        scanf("%d %d", &A, &B);
        printf("%d\n", A + B);
    }
    
    return 0;
}
