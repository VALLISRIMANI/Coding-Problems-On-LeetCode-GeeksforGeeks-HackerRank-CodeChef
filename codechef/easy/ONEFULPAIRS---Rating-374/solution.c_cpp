#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    
    // Check if (a, b) is a Oneful Pair
    if (a + b + (a * b) == 111) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }

    return 0;
}