#include <stdio.h>

int main() {
    int a, b, c; // side lengths of triangle
    scanf("%d %d %d", &a, &b, &c);
    // Complete the code
    
    if ( a + b > c || a + c > b || b + c > a) {
        printf("YES");
    } else {
        printf("NO");
    }
}
