#include <stdio.h>

int main() {
    // write your code here
    int num;
    scanf("%d", &num);
    
    if (num % 400 == 0 || num % 4 == 0 && num % 100 != 0) {
        printf("YES");
    } else {
        printf("NO");
    }
}
