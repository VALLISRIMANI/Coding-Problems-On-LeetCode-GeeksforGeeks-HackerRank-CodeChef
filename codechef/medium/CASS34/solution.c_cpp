#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    // Complete the code
    int sum = 0;
    
    while (n > 0) {
        sum += n % 10;
        n /= 10;
    }
    
    printf("%d", sum);
}
