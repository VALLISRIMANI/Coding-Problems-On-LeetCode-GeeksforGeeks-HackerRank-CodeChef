#include <stdio.h>

int main() {
	// your code goes here
    int num;
    scanf("%d", &num);
    int sum = 0;
    
    while (num > 0) {
        if (num % 2 == 0) sum += num;
        num--;
    }
    
    printf("%d", sum);
}

