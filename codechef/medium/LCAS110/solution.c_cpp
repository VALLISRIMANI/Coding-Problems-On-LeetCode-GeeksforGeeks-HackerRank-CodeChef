#include <stdio.h>

int main() {
	// your code goes here
    int num;
    scanf("%d", &num);
    
    int reverse = 0;
    
    while (num != 0) {
        reverse = reverse * 10 + (num % 10);
        num /= 10;
    }
    
    printf("%d", reverse);
}

