#include <stdio.h>

int fibonacci(int n) {
    if (n <= 1) {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}

int main() {
    int terms;
    scanf("%d", &terms);
    printf("%d",fibonacci(terms));

    return 0;
}
