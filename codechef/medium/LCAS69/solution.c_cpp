#include <stdio.h>

int main() {
	// your code goes here
    int num;
    scanf("%d", &num);
    
    if (num >= 10 && num <= 20) {
        printf("In Range");
    } else {
        printf("Out of Range");
    }
}

